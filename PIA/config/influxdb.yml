apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: Cryptobro_Together_Strong
      defaultBucket: livemarket
    secureJsonData:
      token: solo_para_cryptobros
    isDefault: true